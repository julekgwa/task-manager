{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna exec yarn install",
    "test": "lerna run test",
    "start": "lerna run start",
    "build": "lerna run build",
    "clean": "lerna run clean"
  }
}
